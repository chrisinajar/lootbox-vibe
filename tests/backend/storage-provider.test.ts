import { describe, it, expect } from '@jest/globals';

describe('StorageProvider batch atomicity', () => {
  it.skip('applies all ops on success', async () => {
    expect(true).toBe(true);
  });

  it.skip('rolls back on failure', async () => {
    expect(true).toBe(true);
  });
});

