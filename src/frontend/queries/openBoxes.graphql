mutation OpenBoxes($input: OpenBoxesInput!) {
  openBoxes(input: $input) {
    stacks { stackId typeId rarity count }
    currencies { currency amount }
    unlocks
    cosmetics { typeId modId modName }
  }
}
