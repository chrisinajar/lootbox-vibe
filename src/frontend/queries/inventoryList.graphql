query InventoryList($filter: InventoryFilter, $limit: Int, $cursor: ID) {
  inventoryList(filter: $filter, limit: $limit, cursor: $cursor) {
    rows { stackId typeId rarity count }
    nextCursor
  }
}

