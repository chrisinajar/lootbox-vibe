# Copy to .env for local development

# API server
API_PORT=4000
CORS_ORIGIN=http://localhost:5173
LOG_LEVEL=info

# Expose config hash when enabled (non-prod)
EXPOSE_CONFIG_HASH=1

# Default fallback user id (when no X-User-Id header)
DEFAULT_UID=anonymous

# Telemetry (JSONL event log)
# Set ELOG_ENABLED=1 and ELOG_PATH to enable writes
ELOG_ENABLED=0
ELOG_PATH=./data/elog.jsonl
# Optional retention for rotated logs (if ELOG_PATH is a directory)
ELOG_KEEP_DAYS=7

# Seed script defaults
SEED_UID=seed-user

# Frontend feature flags (Vite)
# Enable developer dashboard route at /#/dev (non-prod default on)
VITE_ENABLE_DEV_UI=1
